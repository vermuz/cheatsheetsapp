import { CheatsheetPipe } from './cheatsheet.pipe';

describe('CheatsheetPipe', () => {
  it('create an instance', () => {
    const pipe = new CheatsheetPipe();
    expect(pipe).toBeTruthy();
  });
});
